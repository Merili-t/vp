<!DOCTYPE html>
<html lang="et">
<head>
	<meta charset="UTF-8">
	<title>Merili Tiik</title>
   
</head>

<body>
       <img src="/banner/vp_banner_2024.png" alt=" veebiprogrammeerimise 2024 aasta kursuse banner">
  
  <h1>Merili Tiik</h1>
  <h2>Tere Tulemast!</h2>																						
  <p>See leht on loodud õppetöö raames <a href="https://www.tlu.ee/"> Tallinna Ülikoolis </a> ning ei sisalda tõsiseltvõetavat materjali!</p>
 
 <hr>
 <ul>
	<li><a href ="/">Avalehele</a></li>
 </ul>
 <hr>
 <h2> lisa filmitegelaste seoseid</h2>
 
 <form method="POST">
    <label for="personSelect">vali filmitegealne</label>
	<br>
	<select id= "personSelect" name= "personSelect">
		<option select disabled> Vali tegelane </option>
		<% personList.forEach(function(person) { %>
			<option value= "<%= person.id %>"><%= person.firstName %> <<%= person.lastName %>, sünd. <%=person.birthOfDate %> </option>
		<% }) %>
	</select>
	<br>
	<label for="movieSelect">Vali film</label>
	<br>
	<select id="movieSelect" name="movieSelect">
		<option selected disabled>Vali film</option>
		<% movieList.forEach(function(movie) { %>
			<option value="<%= movie.id %>"><%= movie.title %>, toodetud: <%= movie.production_year %></option>
		<% }); %>
	</select>
	<br>
	<label for="positionSelect">Vali roll</label>
	<br>
	<select id="positionSelect" name="positionSelect">
		<option selected disabled>Vali roll</option>
		<% positionList.forEach(function(position) { %>
			<option value="<%= position.id %>"><%= position.positionName %></option>
		<% }); %>
	</select>
		<label for="roleInput">Roll/tegelaskuju (kui on näitleja)</label>
		<input type="text" id="roleInput" name="roleInput" placeholder="Tegelaskuju ...">
	<br>
    <input type="submit" id="relationSubmit" name="relationSubmit" value="Salvesta">
 </form>

</body>
</html>